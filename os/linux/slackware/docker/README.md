# `os/linux/slackware/docker/`

## About
This directory contains some scripts useful for building a Docker system package for Slackware Linux.

## Usage
 - To build the package, run:
   ```bash
   make build
   ```
 - To install the package, run:
   ```bash
   make install
   ```
